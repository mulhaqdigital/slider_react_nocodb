{
  "name": "notion-cms",
  "version": "1.0.0",
  "description": "Notion as a Headless CMS",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "build": "mkdir -p dist && cp -r src/* dist/"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.16",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.18.2",
    "node-cache": "^5.1.2",
    "swr": "^2.3.3"
  }
}
